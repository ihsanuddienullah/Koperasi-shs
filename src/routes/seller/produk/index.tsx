import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/seller/produk/')({
  component: () => <div>Manajemen Produk Seller — akan dibangun di Prompt 2</div>,
})
